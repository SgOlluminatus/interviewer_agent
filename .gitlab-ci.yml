stages:
    - deploy

test:
    stage: deploy
    script:
        - pwd
        - whoami
        - kill $(pgrep -f streamlit)
        - cp /home/tsogjavkhlan_odbayar/interviewer/.env .
        - source /home/tsogjavkhlan_odbayar/interviewer/interviewer/bin/activate
        - nohup streamlit run interviewer.py > log.out 2> log.err < /dev/null &
    only:
        - main